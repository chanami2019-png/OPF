do local Players=game:GetService("Players");local LocalPlayer=Players.LocalPlayer;local Whitelist={[10875761174]=true,[2015984957]=true,[5821814221]=true,[142029241]=true,[225027071]=true,[815033308]=true,[3131317360]=true,[1026648568]=true,[1177804775]=true,[1129866707]=true,[1786575446]=true,[893143981]=true,[11038275683]=true,[1519445664]=true,[7139453193]=true,[6021717105]=true,[7154673109]=true,[1299679685]=true,[10128798773]=true,[2875777516]=true,[2694368726]=true};if  not Whitelist[LocalPlayer.UserId] then warn("Not allowed");return;end local Fluent=loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))();local SaveManager=loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))();local InterfaceManager=loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"))();local Window=Fluent:CreateWindow({Title="Fluent "   .. Fluent.Version ,SubTitle="by dawid",TabWidth=120,Size=UDim2.fromOffset(420,360),Acrylic=false,Theme="Dark",MinimizeKey=Enum.KeyCode.LeftAlt});local Tabs={Main=Window:AddTab({Title="Main",Icon=""}),Main4=Window:AddTab({Title="Sam",Icon=""}),Island=Window:AddTab({Title="Island",Icon=""}),Quest=Window:AddTab({Title="Quest",Icon=""}),Settings=Window:AddTab({Title="Settings",Icon="settings"})};local Options=Fluent.Options;local Players=game:GetService("Players");local LocalPlayer=Players.LocalPlayer;local function getconnect(targetButton) local events={"Activated","MouseButton1Down","MouseButton1Click","MouseButton1Up"};for _,eventName in next,events do pcall(function() for _,connection in next,getconnections(targetButton[eventName]) do pcall(function() connection.Function();end);end end);end end local RunService=game:GetService("RunService");local Players=game:GetService("Players");local UserInputService=game:GetService("UserInputService");local VirtualInputManager=game:GetService("VirtualInputManager");local LocalPlayer=Players.LocalPlayer;local Players=game:GetService("Players");local VirtualInputManager=game:GetService("VirtualInputManager");local PlayerGui=Players.LocalPlayer:WaitForChild("PlayerGui");local ScreenGui=Instance.new("ScreenGui");ScreenGui.Name="MobileControlButton";ScreenGui.ResetOnSpawn=false;ScreenGui.Parent=PlayerGui;local Button=Instance.new("TextButton");Button.Parent=ScreenGui;Button.Size=UDim2.fromOffset(55,55);Button.AnchorPoint=Vector2.new(1,0);Button.Position=UDim2.new(1, -20,0,80);Button.BackgroundColor3=Color3.fromRGB(40,40,40);Button.BackgroundTransparency=0.2;Button.BorderSizePixel=0;Button.Text="Ctrl";Button.TextScaled=true;Button.TextColor3=Color3.new(1,1,1);local Corner=Instance.new("UICorner");Corner.CornerRadius=UDim.new(1,0);Corner.Parent=Button;Button.MouseButton1Click:Connect(function() VirtualInputManager:SendKeyEvent(true,Enum.KeyCode.LeftControl,false,game);task.wait(0.05);VirtualInputManager:SendKeyEvent(false,Enum.KeyCode.LeftControl,false,game);end);local function isBlacklisted(name) local blacklist={"Lv2000 Crocodile","Lv20000 Whitebeard"};for _,bname in ipairs(blacklist) do if (name==bname) then return true;end end return false;end local Players=game:GetService("Players");local LocalPlayer=Players.LocalPlayer;_G.AutoEquipTool=false;_G.SelectedToolName=nil;local function GetToolList() local tools={};local Backpack=LocalPlayer:FindFirstChild("Backpack");local Character=LocalPlayer.Character;if Backpack then for _,item in ipairs(Backpack:GetChildren()) do if item:IsA("Tool") then table.insert(tools,item.Name);end end end if Character then for _,item in ipairs(Character:GetChildren()) do if item:IsA("Tool") then table.insert(tools,item.Name);end end end if ( #tools==0) then table.insert(tools,"ไม่มีของ");end return tools;end local ToolDropdown=Tabs.Main:AddDropdown("ToolDropdown",{Title="เลือกของที่จะถือ",Values=GetToolList(),Multi=false,Default=1});_G.SelectedToolName=ToolDropdown.Value;ToolDropdown:OnChanged(function(value) _G.SelectedToolName=value;end);local RefreshButton=Tabs.Main:AddButton({Title="รีเฟรชรายการของ",Description="เช็คของในตัวและ Backpack ใหม่",Callback=function() local newList=GetToolList();ToolDropdown:SetValues(newList);ToolDropdown:SetValue(newList[1]);_G.SelectedToolName=newList[1];end});local function EquipSelectedTool() if ( not _G.SelectedToolName or (_G.SelectedToolName=="ไม่มีของ")) then return;end local Character=LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() ;local Humanoid=Character:FindFirstChildOfClass("Humanoid");local Backpack=LocalPlayer:FindFirstChild("Backpack");if ( not Humanoid or  not Backpack) then return;end local Tool=Backpack:FindFirstChild(_G.SelectedToolName) or Character:FindFirstChild(_G.SelectedToolName) ;if (Tool and Tool:IsA("Tool")) then pcall(function() Humanoid:EquipTool(Tool);end);end end local AutoEquipToggle=Tabs.Main:AddToggle("AutoEquipTool",{Title="Auto Equip Tool",Default=false});AutoEquipToggle:OnChanged(function(state) _G.AutoEquipTool=state;if state then task.spawn(function() while _G.AutoEquipTool do EquipSelectedTool();task.wait(0.3);end end);else end end);AutoEquipToggle:SetValue(false);local function clickCenter() local camera=workspace.CurrentCamera;if  not camera then return;end local x=camera.ViewportSize.X/4 ;local y=camera.ViewportSize.Y/4 ;pcall(function() VirtualInputManager:SendMouseButtonEvent(x,y,0,true,game,0);task.wait();VirtualInputManager:SendMouseButtonEvent(x,y,0,false,game,0);end);end local ClickToggle=Tabs.Main:AddToggle("AutoClick",{Title="ออโต้กด",Default=false});_G.AutoClick=false;ClickToggle:OnChanged(function(state) _G.AutoClick=state;if _G.AutoClick then task.spawn(function() while _G.AutoClick do clickCenter();task.wait(0.2);end end);end end);LocalPlayer.CharacterAdded:Connect(function() task.wait(1);local newList=GetToolList();ToolDropdown:SetValues(newList);if _G.SelectedToolName then ToolDropdown:SetValue(_G.SelectedToolName);end end);local function isTargetMob(name) if isBlacklisted(name) then return false;end local levelStr=string.match(name,"^Lv(%d+)");if levelStr then local level=tonumber(levelStr);if (level and (level>=19)) then return true;else return false;end end return false;end local FarmToggle=Tabs.Main:AddToggle("AutoFarm",{Title="Auto Farm",Default=false});_G.AutoFarm=false;FarmToggle:OnChanged(function(state) _G.AutoFarm=state;if _G.AutoFarm then task.spawn(function() while _G.AutoFarm do local Character=LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() ;local HRP=Character:WaitForChild("HumanoidRootPart");local Humanoid=Character:WaitForChild("Humanoid");local Backpack=LocalPlayer:WaitForChild("Backpack");local Tool=Character:FindFirstChild("Kogatana") or Backpack:FindFirstChild("Kogatana") ;if (Tool and Tool:IsA("Tool")) then pcall(function() Humanoid:EquipTool(Tool);end);end local aliveFolder=workspace:FindFirstChild("Alive");if aliveFolder then for _,mob in ipairs(aliveFolder:GetChildren()) do if  not _G.AutoFarm then break;end if (mob and mob.Parent and isTargetMob(mob.Name)) then local mobHumanoid=mob:FindFirstChildOfClass("Humanoid");local mobHRP=mob:FindFirstChild("HumanoidRootPart");if (mobHumanoid and mobHRP and (mobHumanoid.Health>0)) then while _G.AutoFarm and Character.Parent and mob.Parent and mobHumanoid and mobHRP and (mobHumanoid.Health>0)  do if  not mobHRP.Parent then break;end if (mobHumanoid.Health<=(mobHumanoid.MaxHealth * 0.7)) then mobHumanoid.Health=0;break;end local behindPos=(mobHRP.Position-(mobHRP.CFrame.LookVector * 2)) + Vector3.new(0,0,0) ;pcall(function() HRP.CFrame=CFrame.new(behindPos) * (mobHRP.CFrame-mobHRP.Position) ;end);local EquippedTool=Character:FindFirstChild("Kogatana");if  not EquippedTool then local ToolInBag=Backpack:FindFirstChild("Kogatana");if ToolInBag then pcall(function() Humanoid:EquipTool(ToolInBag);end);end end pcall(function() local CurrentTool=Character:FindFirstChild("Kogatana");if CurrentTool then CurrentTool:Activate();end end);task.wait();mobHumanoid=mob:FindFirstChildOfClass("Humanoid");mobHRP=mob:FindFirstChild("HumanoidRootPart");if ( not mobHumanoid or  not mobHRP) then break;end end task.wait();end end end else task.wait();end task.wait();end end);else end end);FarmToggle:SetValue(false);local Toggle=Tabs.Main:AddToggle("AutoPressLoad",{Title="Auto Spawn",Default=false});_G.AutoPressLoad=false;Toggle:OnChanged(function(state) _G.AutoPressLoad=state;if _G.AutoPressLoad then task.spawn(function() while _G.AutoPressLoad do local player=game:GetService("Players").LocalPlayer;local loadButton=nil;pcall(function() loadButton=player.PlayerGui:WaitForChild("Load",3);if loadButton then loadButton=loadButton.Frame:WaitForChild("Load",3);end end);if loadButton then local function getconnect(obj) for _,con in pairs(getconnections(obj.Activated)) do return con.Function;end return nil;end local func=getconnect(loadButton);if func then pcall(function() func();end);elseif loadButton.Fire then pcall(function() loadButton:Fire();end);end else warn("Load button not found");end task.wait(1);end end);else end end);Toggle:SetValue(false);local Toggle=Tabs.Main:AddToggle("AutoJoeQuest",{Title="Auto Respawn",Default=false});_G.AutoRe=false;Toggle:OnChanged(function(state) _G.AutoRe=state;if state then task.spawn(function() while _G.AutoRe do local Character=LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() ;local Humanoid=Character:FindFirstChildOfClass("Humanoid");if (Humanoid and (Humanoid.Health>0)) then Humanoid.Health=0;end LocalPlayer.CharacterAdded:Wait();task.wait(1);end end);else end end);Toggle:SetValue(false);local VirtualInputManager=game:GetService("VirtualInputManager");local LocalPlayer=game:GetService("Players").LocalPlayer;local Toggle=Tabs.Main:AddToggle("AutoRHakiToggle",{Title="Auto R (Haki)",Default=false});_G.AutoR=false;local hasUsedHaki=false;local function getHakiPercent() local playerGui=LocalPlayer:WaitForChild("PlayerGui");local hakiFrame=playerGui.HealthBar.Frame:FindFirstChild("Haki") and playerGui.HealthBar.Frame.Haki:FindFirstChild("Frame") ;if  not hakiFrame then return 0;end local absSize=hakiFrame.AbsoluteSize;local maxWidth=150;local currentWidth=absSize.X;local percent=(currentWidth/maxWidth) * 100 ;return percent;end local function isObservationActive() local statusFrame=LocalPlayer.PlayerGui.HealthBar.Frame:FindFirstChild("Status");if statusFrame then local obs=statusFrame:FindFirstChild("Observation");if (obs and obs.Visible) then return true;end end return false;end Toggle:OnChanged(function(state) _G.AutoR=state;if state then hasUsedHaki=false;task.spawn(function() while _G.AutoR do local percent=getHakiPercent();if ( not hasUsedHaki and (percent>=80)) then repeat VirtualInputManager:SendKeyEvent(true,Enum.KeyCode.R,false,game);task.wait(0.05);VirtualInputManager:SendKeyEvent(false,Enum.KeyCode.R,false,game);task.wait(0.1);until isObservationActive() or  not _G.AutoR  if isObservationActive() then hasUsedHaki=true;else break;end elseif (hasUsedHaki and (percent<=5)) then hasUsedHaki=false;end task.wait(1);end end);else hasUsedHaki=false;end end);Toggle:SetValue(false);local Toggle=Tabs.Main:AddToggle("AutoWarpChestSpawner",{Title="Tp ChestSpawner",Default=false});local Players=game:GetService("Players");local LocalPlayer=Players.LocalPlayer;_G.AutoWarpChestSpawner=false;Toggle:OnChanged(function() _G.AutoWarpChestSpawner=Toggle.Value;if _G.AutoWarpChestSpawner then task.spawn(function() while _G.AutoWarpChestSpawner do local Character=LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() ;local HRP=Character:WaitForChild("HumanoidRootPart");local chestsFolder=workspace:FindFirstChild("Chests");if chestsFolder then local targets={};for _,obj in ipairs(chestsFolder:GetChildren()) do if ((obj.Name=="ChestSpawner") and obj:IsA("Model")) then local treasureChest=obj:FindFirstChild("TreasureChest");if (treasureChest and treasureChest:IsA("BasePart")) then table.insert(targets,treasureChest);elseif (treasureChest and treasureChest:IsA("Model")) then local partToWarp=treasureChest.PrimaryPart;if partToWarp then table.insert(targets,partToWarp);end end elseif ((obj.Name=="ChestSpawner") and obj:IsA("BasePart")) then table.insert(targets,obj);end end for _,targetPart in ipairs(targets) do if  not _G.AutoWarpChestSpawner then break;end if (targetPart and targetPart.Parent) then local frontOffset=targetPart.CFrame.LookVector;pcall(function() HRP.CFrame=CFrame.new(targetPart.Position + frontOffset + Vector3.new(0,0,0) ,targetPart.Position);end);task.wait(0.2);end end else task.wait(1);end task.wait(0.2);end end);end end);local Toggle=Tabs.Main:AddToggle("BarrelsCratesJuicingToggle",{Title="Barrels",Default=false});local Players=game:GetService("Players");local LocalPlayer=Players.LocalPlayer;_G.Barrels=false;Toggle:OnChanged(function() _G.Barrels=Toggle.Value;if _G.Barrels then task.spawn(function() local juicingClickDetector=nil;pcall(function() local kitchenChildren=workspace.MapFolder.Island8.Kitchen:GetChildren();if (kitchenChildren[2] and kitchenChildren[2]:FindFirstChild("JuicingBowl")) then local bowl=kitchenChildren[2].JuicingBowl:FindFirstChild("Bowl");if bowl then juicingClickDetector=bowl:FindFirstChildWhichIsA("ClickDetector");end end end);local lastJuicingClick=0;while _G.Barrels do local Character=LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() ;local HRP=Character:WaitForChild("HumanoidRootPart");local barrelsFolder=workspace:FindFirstChild("Barrels");local targets={};if barrelsFolder then local barrelsList=barrelsFolder:FindFirstChild("Barrels");if barrelsList then for _,barrel in ipairs(barrelsList:GetChildren()) do if barrel:IsA("BasePart") then table.insert(targets,barrel);end end end local cratesList=barrelsFolder:FindFirstChild("Crates");if cratesList then for _,crate in ipairs(cratesList:GetChildren()) do if crate:IsA("BasePart") then table.insert(targets,crate);end end end end for _,target in ipairs(targets) do if  not _G.Barrels then break;end if (target and target.Parent) then local frontOffset=target.CFrame.LookVector * 3 ;pcall(function() HRP.CFrame=CFrame.new(target.Position + frontOffset + Vector3.new(0,2,0) );end);task.wait(0.12);local clickDetector=target:FindFirstChildWhichIsA("ClickDetector",true);if clickDetector then for i=1,20 do if  not _G.Barrels then break;end pcall(function() fireclickdetector(clickDetector);end);task.wait();end end task.wait();end end if (juicingClickDetector and ((os.clock() -lastJuicingClick)>=60)) then pcall(function() fireclickdetector(juicingClickDetector);end);lastJuicingClick=os.clock();end task.wait();end end);end end);local Toggle=Tabs.Main:AddToggle("AutoEatJuiceFast",{Title="Auto Eat Juice",Default=false});local Players=game:GetService("Players");local VirtualInputManager=game:GetService("VirtualInputManager");local LocalPlayer=Players.LocalPlayer;_G.AutoEatJuiceFast=false;local function ClickCenter() local Camera=workspace.CurrentCamera;if  not Camera then return;end local Size=Camera.ViewportSize;local X=Size.X/2 ;local Y=Size.Y/2 ;VirtualInputManager:SendMouseButtonEvent(X,Y,0,true,game,0);task.wait();VirtualInputManager:SendMouseButtonEvent(X,Y,0,false,game,0);end local function UseJuice(tool) local Character=LocalPlayer.Character;if  not Character then return;end local Humanoid=Character:FindFirstChildOfClass("Humanoid");if  not Humanoid then return;end pcall(function() Humanoid:EquipTool(tool);end);task.wait(0.03);pcall(function() tool:Activate();end);task.wait(0.03);pcall(function() ClickCenter();end);task.wait(0.05);pcall(function() Humanoid:UnequipTools();end);end Toggle:OnChanged(function(state) _G.AutoEatJuiceFast=state;if state then task.spawn(function() while _G.AutoEatJuiceFast do local Character=LocalPlayer.Character;local Backpack=LocalPlayer:FindFirstChild("Backpack");if (Character and Backpack) then local JuiceItems={};for _,v in ipairs(Backpack:GetChildren()) do if v:IsA("Tool") then local name=string.lower(v.Name);if (string.find(name,"juice") or string.find(name,"milk")) then table.insert(JuiceItems,v);end end end for _,v in ipairs(Character:GetChildren()) do if v:IsA("Tool") then local name=string.lower(v.Name);if (string.find(name,"juice") or string.find(name,"milk")) then table.insert(JuiceItems,v);end end end for _,Tool in ipairs(JuiceItems) do if  not _G.AutoEatJuiceFast then break;end if (Tool and Tool.Parent) then UseJuice(Tool);end end end task.wait(0.1);end end);end end);Toggle:SetValue(false);Toggle:SetValue(false);local Toggle=Tabs.Main:AddToggle("AutoGoldenApple",{Title="Auto Golden Apple",Default=false});_G.AutoGoldenApple=false;local function getCharacter() return LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() ;end local function findApple() local char=LocalPlayer.Character;local backpack=LocalPlayer:FindFirstChild("Backpack");if char then for _,v in pairs(char:GetChildren()) do if (v:IsA("Tool") and (v.Name=="Golden Apple")) then return v;end end end if backpack then for _,v in pairs(backpack:GetChildren()) do if (v:IsA("Tool") and (v.Name=="Golden Apple")) then return v;end end end return nil;end Toggle:OnChanged(function() _G.AutoGoldenApple=Options.AutoGoldenApple.Value;if _G.AutoGoldenApple then task.spawn(function() while _G.AutoGoldenApple do local char=getCharacter();local humanoid=char:WaitForChild("Humanoid");local apple=findApple();if apple then apple.Parent=char;task.wait(0.1);pcall(function() apple:Activate();end);end task.wait(0.2);end end);LocalPlayer.CharacterAdded:Connect(function() task.wait(1);end);end end);local Toggle=Tabs.Main:AddToggle("AutoSamQuest",{Title="Auto Sam Quest",Default=false});_G.AutoSamQuest=false;Toggle:OnChanged(function() _G.AutoSamQuest=Toggle.Value;if _G.AutoSamQuest then task.spawn(function() while _G.AutoSamQuest do local player=game:GetService("Players").LocalPlayer;local character=player.Character or player.CharacterAdded:Wait() ;local humanoid=character:FindFirstChildOfClass("Humanoid");while humanoid and (humanoid.Health<=0)  do task.wait();character=player.Character or player.CharacterAdded:Wait() ;humanoid=character:FindFirstChildOfClass("Humanoid");end local gui=player:WaitForChild("PlayerGui");local samReady=false;repeat samReady=false;pcall(function() local timer=gui.Menu.Frame.MenuList.Stats.Frame.A.Sam.SamTimer;if (timer and string.find(timer.Text,"Ready!")) then samReady=true;end end);task.wait();until samReady or  not _G.AutoSamQuest  if  not _G.AutoSamQuest then break;end local sam=workspace.Ignore.NPCs.DailyQuest:FindFirstChild("Sam");if sam then if (player and player.Character and player.Character:FindFirstChild("HumanoidRootPart") and sam:FindFirstChild("HumanoidRootPart")) then player.Character.HumanoidRootPart.CFrame=sam.HumanoidRootPart.CFrame * CFrame.new(0,0,3) ;end end local dialogue=gui:FindFirstChild("QuestGui") and gui.QuestGui:FindFirstChild("Dialogue") ;if (dialogue and dialogue.Visible) then pcall(function() local option=dialogue.Options:FindFirstChild("Option");if (option and option.Visible) then getconnect(option);task.wait();getconnect(option);task.wait();getconnect(option);end end);task.wait();pcall(function() local leave=dialogue.Options:FindFirstChild("Leave");if leave then getconnect(leave);end end);elseif sam then for _,v in pairs(sam:GetDescendants()) do if v:IsA("ProximityPrompt") then pcall(function() v.MaxActivationDistance=math.huge;v.HoldDuration=0;v.RequiresLineOfSight=false;fireproximityprompt(v);end);end if v:IsA("ClickDetector") then pcall(function() fireclickdetector(v);end);end end end task.wait();end end);end end);Options.AutoSamQuest:SetValue(false);local AutoDropCompass=false;local DropToggle=Tabs.Main4:AddToggle("AutoDropCompass",{Title="Auto Drop Compass",Default=false});local function DropCompass() local Character=LocalPlayer.Character;local Backpack=LocalPlayer:FindFirstChild("Backpack");local Compass=nil;if Character then Compass=Character:FindFirstChild("Compass");end if ( not Compass and Backpack) then Compass=Backpack:FindFirstChild("Compass");end if (Compass and Compass:IsA("Tool")) then local Humanoid=Character and Character:FindFirstChildOfClass("Humanoid") ;if Humanoid then pcall(function() Humanoid:EquipTool(Compass);end);end task.wait(0.2);pcall(function() Compass.Parent=workspace;end);end end DropToggle:OnChanged(function(state) AutoDropCompass=state;if state then task.spawn(function() while AutoDropCompass do DropCompass();task.wait(0.5);end end);else end end);DropToggle:SetValue(false);local Players=game:GetService("Players");local LocalPlayer=Players.LocalPlayer;local PickupToggle=Tabs.Main4:AddToggle("AutoPickupCompass",{Title="Auto Compass",Default=false});_G.AutoPickupCompass=false;local function isInsidePlayerCharacter(obj) local current=obj;while current and (current~=workspace)  do for _,plr in ipairs(Players:GetPlayers()) do if (plr.Character and (current==plr.Character)) then return true;end end current=current.Parent;end return false;end local function moveCompassToPlayer(compass) local Character=LocalPlayer.Character;if  not Character then return;end local HRP=Character:FindFirstChild("HumanoidRootPart");if  not HRP then return;end if ( not compass or  not compass.Parent) then return;end if isInsidePlayerCharacter(compass) then return;end local part=nil;if compass:IsA("BasePart") then part=compass;elseif compass:IsA("Tool") then part=compass:FindFirstChild("Handle") or compass:FindFirstChildWhichIsA("BasePart",true) ;elseif compass:IsA("Model") then part=compass.PrimaryPart or compass:FindFirstChildWhichIsA("BasePart",true) ;end if part then pcall(function() part.CanCollide=false;part.CFrame=HRP.CFrame;end);end end local function PickupCompass() for _,obj in ipairs(workspace:GetChildren()) do if  not _G.AutoPickupCompass then break;end if ((obj.Name=="Compass") and  not isInsidePlayerCharacter(obj)) then moveCompassToPlayer(obj);end end end PickupToggle:OnChanged(function(state) _G.AutoPickupCompass=state;if state then task.spawn(function() while _G.AutoPickupCompass do pcall(PickupCompass);task.wait(0.1);end end);end end);PickupToggle:SetValue(false);local Islands={{name="Sam",cf=CFrame.new( -1303,217, -1272.21)},{name="Island Town",cf=CFrame.new( -200.01,225.12, -1038.5)},{name="Vokun Field",cf=CFrame.new(4590.82,216.99,4959.73)},{name="Strange Tent",cf=CFrame.new(1256.88,224, -3282.47)},{name="Rocky Islands",cf=CFrame.new(4371.24,318.81, -3234.06)},{name="Jail Islands",cf=CFrame.new( -2756.56,216, -991.36)},{name="Island Tree A",cf=CFrame.new(1097.95,217,3315.32)},{name="Snowy Mountains",cf=CFrame.new(6601.46,302.67, -1551.53)},{name="Island Snowy",cf=CFrame.new( -1899.04,222,3351.54)},{name="Sand Castle",cf=CFrame.new(1021.73,224, -3334.99)},{name="Island Rocky",cf=CFrame.new( -31.82,229,2158.42)},{name="Island Mountain",cf=CFrame.new(2053,490, -666.88)},{name="Island Grassy",cf=CFrame.new(704.03,241.2,1194.62)},{name="Island Forest",cf=CFrame.new( -6015.06,402,21.62)},{name="Island Evil",cf=CFrame.new( -5273.38,519.5, -7846.25)},{name="Island Crescent",cf=CFrame.new(3396.06,217,1611.94)},{name="Snipers",cf=CFrame.new( -1042.94,355.99,1681.47)},{name="Bar",cf=CFrame.new(1512.06,259.39,2171.05)},{name="Pyramid",cf=CFrame.new(859.67,238.69,5255.51)},{name="Crabs",cf=CFrame.new(1154.78,253, -220.35)},{name="Marine Ford",cf=CFrame.new( -2822.19,299.75, -3631.12)},{name="Island 22",cf=CFrame.new( -5425.64,220, -7708.75)},{name="Cooker Island",cf=CFrame.new(1946.09,217,626.3)},{name="Island 24",cf=CFrame.new( -2788.51,251.56,1008.3)}};for _,island in ipairs(Islands) do Tabs.Island:AddButton({Title=island.name,Callback=function() local Character=LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() ;local HRP=Character:FindFirstChild("HumanoidRootPart");if HRP then pcall(function() HRP.CFrame=island.cf;end);end end});end local Toggle=Tabs.Quest:AddToggle("AutoTraceurQuest",{Title="Auto Traceur Quest",Default=false});_G.AutoTraceurQuest=false;Toggle:OnChanged(function(state) _G.AutoTraceurQuest=state;if state then task.spawn(function() while _G.AutoTraceurQuest do local player=game:GetService("Players").LocalPlayer;local character=player.Character or player.CharacterAdded:Wait() ;local humanoid=character:FindFirstChildOfClass("Humanoid");while humanoid and (humanoid.Health<=0)  do task.wait();character=player.Character or player.CharacterAdded:Wait() ;humanoid=character:FindFirstChildOfClass("Humanoid");end local gui=player:WaitForChild("PlayerGui");local function hasQuest() local ok,result=pcall(function() return gui.QuestGui.QuestsFrame.QuestName.Text;end);return ok and (result=="Bridge Challenge") ;end local function questDone() local ok,result=pcall(function() return gui.QuestGui.QuestsFrame.QuestsScroll:GetChildren()[5].Text;end);if  not ok then return false;end return string.find(result,"1/1")~=nil ;end if  not hasQuest() then local npc=workspace.Ignore.NPCs.Quest:FindFirstChild("Traceur");if npc then if (character:FindFirstChild("HumanoidRootPart") and npc:FindFirstChild("HumanoidRootPart")) then character.HumanoidRootPart.CFrame=npc.HumanoidRootPart.CFrame * CFrame.new(0,0,3) ;end task.wait(0.5);end repeat if  not _G.AutoTraceurQuest then break;end if npc then for _,v in pairs(npc:GetDescendants()) do if v:IsA("ProximityPrompt") then pcall(function() v.MaxActivationDistance=math.huge;v.HoldDuration=0;v.RequiresLineOfSight=false;fireproximityprompt(v);end);end if v:IsA("ClickDetector") then pcall(function() fireclickdetector(v);end);end end end local dialogue=gui:FindFirstChild("QuestGui") and gui.QuestGui:FindFirstChild("Dialogue") ;if (dialogue and dialogue.Visible) then pcall(function() local option=dialogue.Options:FindFirstChild("Option");if (option and option.Visible) then getconnect(option);task.wait(0.2);getconnect(option);task.wait(0.2);getconnect(option);end end);task.wait(0.3);pcall(function() local leave=dialogue.Options:FindFirstChild("Leave");if leave then getconnect(leave);end end);end task.wait(0.5);until hasQuest() or  not _G.AutoTraceurQuest  if  not _G.AutoTraceurQuest then break;end end task.wait(1);local hrp=character:FindFirstChild("HumanoidRootPart");if hrp then pcall(function() hrp.CFrame=CFrame.new( -291.17,306.12, -597.45);end);end task.wait(1);if hrp then pcall(function() hrp.CFrame=CFrame.new( -290.09,306.12, -577.72);end);end repeat task.wait(0.5);until questDone() or  not _G.AutoTraceurQuest  if  not _G.AutoTraceurQuest then break;end task.wait(0.5);local npc=workspace.Ignore.NPCs.Quest:FindFirstChild("Traceur");if npc then if (character:FindFirstChild("HumanoidRootPart") and npc:FindFirstChild("HumanoidRootPart")) then character.HumanoidRootPart.CFrame=npc.HumanoidRootPart.CFrame * CFrame.new(0,0,3) ;end task.wait(0.5);end local dialogue=gui:FindFirstChild("QuestGui") and gui.QuestGui:FindFirstChild("Dialogue") ;if  not (dialogue and dialogue.Visible) then if npc then for _,v in pairs(npc:GetDescendants()) do if v:IsA("ProximityPrompt") then pcall(function() v.MaxActivationDistance=math.huge;v.HoldDuration=0;v.RequiresLineOfSight=false;fireproximityprompt(v);end);end if v:IsA("ClickDetector") then pcall(function() fireclickdetector(v);end);end end task.wait(0.5);end end pcall(function() local option=gui.QuestGui.Dialogue.Options:FindFirstChild("Option");if (option and option.Visible) then getconnect(option);end end);task.wait(0.3);pcall(function() local leave=gui.QuestGui.Dialogue.Options:FindFirstChild("Leave");if leave then getconnect(leave);end end);task.wait(1);end end);end end);Toggle:SetValue(false);local HumbleToggle=Tabs.Quest:AddToggle("AutoHumbleMan1",{Title="Auto Humble Man #1",Default=false});_G.AutoHumbleMan1=false;HumbleToggle:OnChanged(function(state) _G.AutoHumbleMan1=state;if state then task.spawn(function() while _G.AutoHumbleMan1 do local player=game:GetService("Players").LocalPlayer;local character=player.Character or player.CharacterAdded:Wait() ;local humanoid=character:FindFirstChildOfClass("Humanoid");while humanoid and (humanoid.Health<=0)  do task.wait();character=player.Character or player.CharacterAdded:Wait() ;humanoid=character:FindFirstChildOfClass("Humanoid");end local gui=player:WaitForChild("PlayerGui");local function hasQuest() local ok,result=pcall(function() return gui.QuestGui.QuestsFrame.QuestName.Text;end);return ok and (result=="Humble Man #1") ;end local requiredItems={{name="Pumpkin",amount=20},{name="Golden Apple",amount=1},{name="Melon",amount=20},{name="Coconut",amount=20},{name="Apple",amount=20},{name="Cantaloupe",amount=20},{name="Green Apple",amount=20},{name="Banana",amount=20}};local function countItem(itemName) local count=0;local backpack=player:FindFirstChild("Backpack");local char=player.Character;if backpack then for _,v in ipairs(backpack:GetChildren()) do if (v.Name==itemName) then count+=1 end end end if char then for _,v in ipairs(char:GetChildren()) do if (v:IsA("Tool") and (v.Name==itemName)) then count+=1 end end end return count;end local function allItemsDone() for _,item in ipairs(requiredItems) do if (countItem(item.name)<item.amount) then return false;end end return true;end local function interactNPC(npc) for _,v in pairs(npc:GetDescendants()) do if v:IsA("ProximityPrompt") then pcall(function() v.MaxActivationDistance=math.huge;v.HoldDuration=0;v.RequiresLineOfSight=false;fireproximityprompt(v);end);end if v:IsA("ClickDetector") then pcall(function() fireclickdetector(v);end);end end end local function warpToNPC(npc) if (character:FindFirstChild("HumanoidRootPart") and npc:FindFirstChild("HumanoidRootPart")) then character.HumanoidRootPart.CFrame=npc.HumanoidRootPart.CFrame * CFrame.new(0,0,3) ;end end if  not hasQuest() then local npc=workspace.Ignore.NPCs.Quest:FindFirstChild("Old Beggar");if npc then warpToNPC(npc);task.wait(0.5);end if  not hasQuest() then local npc=workspace.Ignore.NPCs.Quest:FindFirstChild("Guard Captain");if npc then warpToNPC(npc);task.wait(0.5);end repeat if  not _G.AutoThiefBeater then break;end if npc then interactNPC(npc);end local dialogue=gui:FindFirstChild("QuestGui") and gui.QuestGui:FindFirstChild("Dialogue") ;if (dialogue and dialogue.Visible) then pcall(function() local option=dialogue.Options:FindFirstChild("Option");if (option and option.Visible) then getconnect(option);task.wait(0.2);getconnect(option);task.wait(0.2);getconnect(option);end end);task.wait(0.3);pcall(function() local leave=dialogue.Options:FindFirstChild("Leave");if leave then getconnect(leave);end end);end task.wait(0.5);until hasQuest() or  not _G.AutoThiefBeater  if  not _G.AutoThiefBeater then break;end end if  not _G.AutoHumbleMan1 then break;end end task.wait(1);while _G.AutoHumbleMan1 and  not allItemsDone()  do character=player.Character or player.CharacterAdded:Wait() ;local HRP=character:FindFirstChild("HumanoidRootPart");local barrelsFolder=workspace:FindFirstChild("Barrels");local targets={};if barrelsFolder then local barrelsList=barrelsFolder:FindFirstChild("Barrels");if barrelsList then for _,barrel in ipairs(barrelsList:GetChildren()) do if barrel:IsA("BasePart") then table.insert(targets,barrel);end end end local cratesList=barrelsFolder:FindFirstChild("Crates");if cratesList then for _,crate in ipairs(cratesList:GetChildren()) do if crate:IsA("BasePart") then table.insert(targets,crate);end end end end for _,target in ipairs(targets) do if ( not _G.AutoHumbleMan1 or allItemsDone()) then break;end if (target and target.Parent) then local frontOffset=target.CFrame.LookVector * 3 ;pcall(function() HRP.CFrame=CFrame.new(target.Position + frontOffset + Vector3.new(0,2,0) );end);task.wait(0.12);local clickDetector=target:FindFirstChildWhichIsA("ClickDetector",true);if clickDetector then for i=1,20 do if ( not _G.AutoHumbleMan1 or allItemsDone()) then break;end pcall(function() fireclickdetector(clickDetector);end);task.wait();end end task.wait();end end task.wait();end if  not _G.AutoHumbleMan1 then break;end task.wait(0.5);local npc=workspace.Ignore.NPCs.Quest:FindFirstChild("Old Beggar");if npc then warpToNPC(npc);task.wait(0.5);interactNPC(npc);task.wait(0.5);end pcall(function() local option2=gui.QuestGui.Dialogue.Options:FindFirstChild("Option2");if (option2 and option2.Visible) then getconnect(option2);end end);task.wait(0.3);if npc then interactNPC(npc);task.wait(0.5);end pcall(function() local option=gui.QuestGui.Dialogue.Options:FindFirstChild("Option");if (option and option.Visible) then getconnect(option);end end);task.wait(0.3);pcall(function() local leave=gui.QuestGui.Dialogue.Options:FindFirstChild("Leave");if leave then getconnect(leave);end end);task.wait(1);task.wait(6);end end);end end);HumbleToggle:SetValue(false);local MargeToggle=Tabs.Quest:AddToggle("AutoMargeQuest",{Title="Auto Marge Quest",Default=false});_G.AutoMargeQuest=false;MargeToggle:OnChanged(function(state) _G.AutoMargeQuest=state;if state then task.spawn(function() while _G.AutoMargeQuest do local player=game:GetService("Players").LocalPlayer;local character=player.Character or player.CharacterAdded:Wait() ;local gui=player:WaitForChild("PlayerGui");local function warpToNPC(npc) if (character:FindFirstChild("HumanoidRootPart") and npc:FindFirstChild("HumanoidRootPart")) then character.HumanoidRootPart.CFrame=npc.HumanoidRootPart.CFrame * CFrame.new(0,0,3) ;end end local function interactNPC(npc) for _,v in pairs(npc:GetDescendants()) do if v:IsA("ProximityPrompt") then pcall(function() v.MaxActivationDistance=math.huge;v.HoldDuration=0;v.RequiresLineOfSight=false;fireproximityprompt(v);end);end if v:IsA("ClickDetector") then pcall(function() fireclickdetector(v);end);end end end local function clickDialogue() local dialogue=gui:FindFirstChild("QuestGui") and gui.QuestGui:FindFirstChild("Dialogue") ;if (dialogue and dialogue.Visible) then pcall(function() local option=dialogue.Options:FindFirstChild("Option");if (option and option.Visible) then getconnect(option);task.wait(0.2);getconnect(option);task.wait(0.2);getconnect(option);end end);task.wait(0.3);pcall(function() local leave=dialogue.Options:FindFirstChild("Leave");if leave then getconnect(leave);end end);end end local marge=workspace.Ignore.NPCs.Quest:FindFirstChild("Marge Nospmis");if marge then warpToNPC(marge);task.wait(0.5);interactNPC(marge);task.wait(0.5);clickDialogue();task.wait(0.5);end local bart=workspace.Ignore.NPCs.Information:FindFirstChild("Bart Nospmis");if bart then warpToNPC(bart);task.wait(0.5);interactNPC(bart);task.wait(0.5);clickDialogue();task.wait(0.5);end if marge then warpToNPC(marge);task.wait(0.5);interactNPC(marge);task.wait(0.5);clickDialogue();task.wait(0.5);end task.wait(1);end end);end end);MargeToggle:SetValue(false);local ThiefToggle=Tabs.Quest:AddToggle("AutoThiefBeater",{Title="Auto Thief Beater",Default=false});_G.AutoThiefBeater=false;ThiefToggle:OnChanged(function(state) _G.AutoThiefBeater=state;if state then task.spawn(function() while _G.AutoThiefBeater do local player=game:GetService("Players").LocalPlayer;local character=player.Character or player.CharacterAdded:Wait() ;local HRP=character:FindFirstChild("HumanoidRootPart");local Humanoid=character:FindFirstChildOfClass("Humanoid");local Backpack=player:FindFirstChild("Backpack");local gui=player:WaitForChild("PlayerGui");local function hasQuest() local ok,result=pcall(function() return gui.QuestGui.QuestsFrame.QuestName.Text;end);return ok and (result=="Thief Beater") ;end local function warpToNPC(npc) if (character:FindFirstChild("HumanoidRootPart") and npc:FindFirstChild("HumanoidRootPart")) then character.HumanoidRootPart.CFrame=npc.HumanoidRootPart.CFrame * CFrame.new(0,0,3) ;end end local function interactNPC(npc) for _,v in pairs(npc:GetDescendants()) do if v:IsA("ProximityPrompt") then pcall(function() v.MaxActivationDistance=math.huge;v.HoldDuration=0;v.RequiresLineOfSight=false;fireproximityprompt(v);end);end if v:IsA("ClickDetector") then pcall(function() fireclickdetector(v);end);end end end local function clickDialogue() local dialogue=gui:FindFirstChild("QuestGui") and gui.QuestGui:FindFirstChild("Dialogue") ;if (dialogue and dialogue.Visible) then pcall(function() local option=dialogue.Options:FindFirstChild("Option");if (option and option.Visible) then getconnect(option);task.wait(0.2);getconnect(option);task.wait(0.2);getconnect(option);end end);task.wait(0.3);pcall(function() local leave=dialogue.Options:FindFirstChild("Leave");if leave then getconnect(leave);end end);end end if  not hasQuest() then local npc=workspace.Ignore.NPCs.Quest:FindFirstChild("Guard Captain");if npc then warpToNPC(npc);task.wait(0.5);interactNPC(npc);task.wait(0.5);clickDialogue();task.wait(0.5);end end local function questDone() local ok,result=pcall(function() return gui.QuestGui.QuestsFrame.QuestsScroll:GetChildren()[5].Text;end);if  not ok then return false;end return string.find(result,"10/10")~=nil ;end while _G.AutoThiefBeater and  not questDone()  do character=player.Character or player.CharacterAdded:Wait() ;HRP=character:FindFirstChild("HumanoidRootPart");Humanoid=character:FindFirstChildOfClass("Humanoid");Backpack=player:FindFirstChild("Backpack");local Tool=character:FindFirstChild("Kogatana") or Backpack:FindFirstChild("Kogatana") ;if (Tool and Tool:IsA("Tool")) then pcall(function() Humanoid:EquipTool(Tool);end);end local aliveFolder=workspace:FindFirstChild("Alive");if aliveFolder then for _,mob in ipairs(aliveFolder:GetChildren()) do if ( not _G.AutoThiefBeater or questDone()) then break;end if string.find(mob.Name,"Thief") then local mobHumanoid=mob:FindFirstChildOfClass("Humanoid");local mobHRP=mob:FindFirstChild("HumanoidRootPart");if (mobHumanoid and mobHRP and (mobHumanoid.Health>0)) then while _G.AutoThiefBeater and  not questDone() and mob.Parent and mobHumanoid and mobHRP and (mobHumanoid.Health>0)  do if  not mobHRP.Parent then break;end if (mobHumanoid.Health<=(mobHumanoid.MaxHealth * 0.7)) then mobHumanoid.Health=0;break;end local behindPos=(mobHRP.Position-(mobHRP.CFrame.LookVector * 1)) + Vector3.new(0,1,0) ;pcall(function() HRP.CFrame=CFrame.new(behindPos) * (mobHRP.CFrame-mobHRP.Position) ;end);local EquippedTool=character:FindFirstChild("Kogatana");if  not EquippedTool then local ToolInBag=Backpack:FindFirstChild("Kogatana");if ToolInBag then pcall(function() Humanoid:EquipTool(ToolInBag);end);end end pcall(function() local CurrentTool=character:FindFirstChild("Kogatana");if CurrentTool then CurrentTool:Activate();end end);task.wait();mobHumanoid=mob:FindFirstChildOfClass("Humanoid");mobHRP=mob:FindFirstChild("HumanoidRootPart");if ( not mobHumanoid or  not mobHRP) then break;end end task.wait();end end end else task.wait(1);end task.wait();end if  not _G.AutoThiefBeater then break;end local npc=workspace.Ignore.NPCs.Quest:FindFirstChild("Guard Captain");if npc then warpToNPC(npc);task.wait(0.5);interactNPC(npc);task.wait(0.5);clickDialogue();end task.wait(1);end end);end end);ThiefToggle:SetValue(false);local FallenToggle=Tabs.Quest:AddToggle("AutoFallenCaptain",{Title="Auto Fallen Captain's Savior",Default=false});_G.AutoFallenCaptain=false;FallenToggle:OnChanged(function(state) _G.AutoFallenCaptain=state;if state then task.spawn(function() while _G.AutoFallenCaptain do local player=game:GetService("Players").LocalPlayer;local character=player.Character or player.CharacterAdded:Wait() ;local HRP=character:FindFirstChild("HumanoidRootPart");local Humanoid=character:FindFirstChildOfClass("Humanoid");local Backpack=player:FindFirstChild("Backpack");local gui=player:WaitForChild("PlayerGui");local function hasQuest() local ok,result=pcall(function() return gui.QuestGui.QuestsFrame.QuestName.Text;end);return ok and (result=="Fallen Captain's Savior") ;end local function warpToNPC(npc) if (character:FindFirstChild("HumanoidRootPart") and npc:FindFirstChild("HumanoidRootPart")) then character.HumanoidRootPart.CFrame=npc.HumanoidRootPart.CFrame * CFrame.new(0,0,3) ;end end local function interactNPC(npc) for _,v in pairs(npc:GetDescendants()) do if v:IsA("ProximityPrompt") then pcall(function() v.MaxActivationDistance=math.huge;v.HoldDuration=0;v.RequiresLineOfSight=false;fireproximityprompt(v);end);end if v:IsA("ClickDetector") then pcall(function() fireclickdetector(v);end);end end end local function clickDialogue() local dialogue=gui:FindFirstChild("QuestGui") and gui.QuestGui:FindFirstChild("Dialogue") ;if (dialogue and dialogue.Visible) then pcall(function() local option=dialogue.Options:FindFirstChild("Option");if (option and option.Visible) then getconnect(option);task.wait(0.2);getconnect(option);task.wait(0.2);getconnect(option);end end);task.wait(0.3);pcall(function() local leave=dialogue.Options:FindFirstChild("Leave");if leave then getconnect(leave);end end);end end local function questDone() local ok,result=pcall(function() return gui.QuestGui.QuestsFrame.QuestsScroll:GetChildren()[5].Text;end);if  not ok then return false;end return string.find(result,"3/3")~=nil ;end local function treasureDone() local ok,result=pcall(function() return gui.QuestGui.QuestsFrame.QuestsScroll:GetChildren()[6].Text;end);if  not ok then return false;end return string.find(result,"1/1")~=nil ;end if  not hasQuest() then local npc=workspace.Ignore.NPCs.Quest:FindFirstChild("Fallen Captain");if npc then warpToNPC(npc);task.wait(0.5);end repeat if  not _G.AutoFallenCaptain then break;end if npc then interactNPC(npc);end local dialogue=gui:FindFirstChild("QuestGui") and gui.QuestGui:FindFirstChild("Dialogue") ;if (dialogue and dialogue.Visible) then pcall(function() local option=dialogue.Options:FindFirstChild("Option");if (option and option.Visible) then getconnect(option);task.wait(0.2);getconnect(option);task.wait(0.2);getconnect(option);end end);task.wait(0.3);pcall(function() local leave=dialogue.Options:FindFirstChild("Leave");if leave then getconnect(leave);end end);end task.wait(0.5);until hasQuest() or  not _G.AutoFallenCaptain  if  not _G.AutoFallenCaptain then break;end end task.wait(1);while _G.AutoFallenCaptain and  not questDone()  do character=player.Character or player.CharacterAdded:Wait() ;HRP=character:FindFirstChild("HumanoidRootPart");Humanoid=character:FindFirstChildOfClass("Humanoid");Backpack=player:FindFirstChild("Backpack");local Tool=character:FindFirstChild("Kogatana") or Backpack:FindFirstChild("Kogatana") ;if (Tool and Tool:IsA("Tool")) then pcall(function() Humanoid:EquipTool(Tool);end);end local aliveFolder=workspace:FindFirstChild("Alive");if aliveFolder then for _,mob in ipairs(aliveFolder:GetChildren()) do if ( not _G.AutoFallenCaptain or questDone()) then break;end if (mob and mob.Parent and string.find(mob.Name,"Bandit")) then local mobHumanoid=mob:FindFirstChildOfClass("Humanoid");local mobHRP=mob:FindFirstChild("HumanoidRootPart");if (mobHumanoid and mobHRP and (mobHumanoid.Health>0)) then while _G.AutoFallenCaptain and  not questDone() and mob.Parent and mobHumanoid and mobHRP and (mobHumanoid.Health>0)  do if  not mobHRP.Parent then break;end if (mobHumanoid.Health<=(mobHumanoid.MaxHealth * 0.7)) then mobHumanoid.Health=0;break;end local behindPos=(mobHRP.Position-(mobHRP.CFrame.LookVector * 1)) + Vector3.new(0,1,0) ;pcall(function() HRP.CFrame=CFrame.new(behindPos) * (mobHRP.CFrame-mobHRP.Position) ;end);local EquippedTool=character:FindFirstChild("Kogatana");if  not EquippedTool then local ToolInBag=Backpack:FindFirstChild("Kogatana");if ToolInBag then pcall(function() Humanoid:EquipTool(ToolInBag);end);end end pcall(function() local CurrentTool=character:FindFirstChild("Kogatana");if CurrentTool then CurrentTool:Activate();end end);task.wait();mobHumanoid=mob:FindFirstChildOfClass("Humanoid");mobHRP=mob:FindFirstChild("HumanoidRootPart");if ( not mobHumanoid or  not mobHRP) then break;end end task.wait();end end end else task.wait(1);end task.wait();end if  not _G.AutoFallenCaptain then break;end while _G.AutoFallenCaptain and  not treasureDone()  do local chest=workspace.Chests:FindFirstChild("Fallen Captain");if chest then local treasureChest=chest:FindFirstChild("TreasureChest");if treasureChest then local part=treasureChest.PrimaryPart or treasureChest:FindFirstChildWhichIsA("BasePart",true) ;if part then pcall(function() character=player.Character or player.CharacterAdded:Wait() ;HRP=character:FindFirstChild("HumanoidRootPart");HRP.CFrame=CFrame.new(part.Position + Vector3.new(0,3,0) );end);end end end task.wait(0.2);end if  not _G.AutoFallenCaptain then break;end local npc=workspace.Ignore.NPCs.Quest:FindFirstChild("Fallen Captain");if npc then warpToNPC(npc);task.wait(0.5);interactNPC(npc);task.wait(0.5);clickDialogue();end task.wait(1);task.wait(6);end end);end end);FallenToggle:SetValue(false);local ExplorerToggle=Tabs.Quest:AddToggle("AutoExplorerQuest",{Title="Auto Explorer Quest",Default=false});_G.AutoExplorerQuest=false;ExplorerToggle:OnChanged(function(state) _G.AutoExplorerQuest=state;if state then task.spawn(function() while _G.AutoExplorerQuest do local player=game:GetService("Players").LocalPlayer;local character=player.Character or player.CharacterAdded:Wait() ;local gui=player:WaitForChild("PlayerGui");local function hasQuest() local ok,result=pcall(function() return gui.QuestGui.QuestsFrame.QuestName.Text;end);return ok and (result=="Adventures") ;end local function warpToNPC(npc) if (character:FindFirstChild("HumanoidRootPart") and npc:FindFirstChild("HumanoidRootPart")) then character.HumanoidRootPart.CFrame=npc.HumanoidRootPart.CFrame * CFrame.new(0,0,3) ;end end local function interactNPC(npc) for _,v in pairs(npc:GetDescendants()) do if v:IsA("ProximityPrompt") then pcall(function() v.MaxActivationDistance=math.huge;v.HoldDuration=0;v.RequiresLineOfSight=false;fireproximityprompt(v);end);end if v:IsA("ClickDetector") then pcall(function() fireclickdetector(v);end);end end end local function clickDialogue() local dialogue=gui:FindFirstChild("QuestGui") and gui.QuestGui:FindFirstChild("Dialogue") ;if (dialogue and dialogue.Visible) then pcall(function() local option=dialogue.Options:FindFirstChild("Option");if (option and option.Visible) then getconnect(option);task.wait(0.2);getconnect(option);task.wait(0.2);getconnect(option);end end);task.wait(0.3);pcall(function() local leave=dialogue.Options:FindFirstChild("Leave");if leave then getconnect(leave);end end);end end local function allIslandsDone() local scroll=gui.QuestGui.QuestsFrame.QuestsScroll;local children=scroll:GetChildren();for i=5,20 do local ok,text=pcall(function() return children[i].Text;end);if  not ok then return false;end if  not string.find(text,"1/1") then return false;end end return true;end local islands={CFrame.new( -1303,217, -1272.21),CFrame.new( -200.01,225.12, -1038.5),CFrame.new(4590.82,216.99,4959.73),CFrame.new(1256.88,224, -3282.47),CFrame.new(4371.24,318.81, -3234.06),CFrame.new( -2756.56,216, -991.36),CFrame.new(1097.95,217,3315.32),CFrame.new(6601.46,302.67, -1551.53),CFrame.new( -1899.04,222,3351.54),CFrame.new(1021.73,224, -3334.99),CFrame.new( -31.82,229,2158.42),CFrame.new(2053,490, -666.88),CFrame.new(704.03,241.2,1194.62),CFrame.new( -6015.06,402,21.62),CFrame.new( -5273.38,519.5, -7846.25),CFrame.new(3396.06,217,1611.94),CFrame.new( -1042.94,355.99,1681.47),CFrame.new(1512.06,259.39,2171.05),CFrame.new(859.67,238.69,5255.51),CFrame.new(1154.78,253, -220.35),CFrame.new( -2822.19,299.75, -3631.12),CFrame.new( -5425.64,220, -7708.75),CFrame.new(1946.09,217,626.3),CFrame.new( -2788.51,251.56,1008.3)};if  not hasQuest() then local npc=workspace.Ignore.NPCs.Quest:FindFirstChild("Explorer");if npc then warpToNPC(npc);task.wait(0.5);end repeat if  not _G.AutoExplorerQuest then break;end if npc then interactNPC(npc);end local dialogue=gui:FindFirstChild("QuestGui") and gui.QuestGui:FindFirstChild("Dialogue") ;if (dialogue and dialogue.Visible) then pcall(function() local option=dialogue.Options:FindFirstChild("Option");if (option and option.Visible) then getconnect(option);task.wait(0.2);getconnect(option);task.wait(0.2);getconnect(option);end end);task.wait(0.3);pcall(function() local leave=dialogue.Options:FindFirstChild("Leave");if leave then getconnect(leave);end end);end task.wait(0.5);until hasQuest() or  not _G.AutoExplorerQuest  if  not _G.AutoExplorerQuest then break;end end task.wait(1);while _G.AutoExplorerQuest and  not allIslandsDone()  do character=player.Character or player.CharacterAdded:Wait() ;local HRP=character:FindFirstChild("HumanoidRootPart");for _,cf in ipairs(islands) do if  not _G.AutoExplorerQuest then break;end if HRP then pcall(function() HRP.CFrame=cf;end);end task.wait(0.5);end task.wait(0.5);end if  not _G.AutoExplorerQuest then break;end local npc=workspace.Ignore.NPCs.Quest:FindFirstChild("Explorer");if npc then warpToNPC(npc);task.wait(0.5);interactNPC(npc);task.wait(0.5);clickDialogue();end task.wait(1);end end);end end);ExplorerToggle:SetValue(false);local ChillToggle=Tabs.Quest:AddToggle("AutoMakeEmChill",{Title="Auto Make'em Chill",Default=false});_G.AutoMakeEmChill=false;ChillToggle:OnChanged(function(state) _G.AutoMakeEmChill=state;if state then task.spawn(function() while _G.AutoMakeEmChill do local player=game:GetService("Players").LocalPlayer;local character=player.Character or player.CharacterAdded:Wait() ;local HRP=character:FindFirstChild("HumanoidRootPart");local Humanoid=character:FindFirstChildOfClass("Humanoid");local Backpack=player:FindFirstChild("Backpack");local gui=player:WaitForChild("PlayerGui");local function hasQuest() local ok,result=pcall(function() return gui.QuestGui.QuestsFrame.QuestName.Text;end);return ok and (result=="Make'em Chill") ;end local function warpToNPC(npc) if (character:FindFirstChild("HumanoidRootPart") and npc:FindFirstChild("HumanoidRootPart")) then character.HumanoidRootPart.CFrame=npc.HumanoidRootPart.CFrame * CFrame.new(0,0,3) ;end end local function interactNPC(npc) for _,v in pairs(npc:GetDescendants()) do if v:IsA("ProximityPrompt") then pcall(function() v.MaxActivationDistance=math.huge;v.HoldDuration=0;v.RequiresLineOfSight=false;fireproximityprompt(v);end);end if v:IsA("ClickDetector") then pcall(function() fireclickdetector(v);end);end end end local function clickDialogue() local dialogue=gui:FindFirstChild("QuestGui") and gui.QuestGui:FindFirstChild("Dialogue") ;if (dialogue and dialogue.Visible) then pcall(function() local option=dialogue.Options:FindFirstChild("Option");if (option and option.Visible) then getconnect(option);task.wait(0.2);getconnect(option);task.wait(0.2);getconnect(option);end end);task.wait(0.3);pcall(function() local leave=dialogue.Options:FindFirstChild("Leave");if leave then getconnect(leave);end end);end end local function questDone() local scroll=gui.QuestGui.QuestsFrame.QuestsScroll;local children=scroll:GetChildren();local bobDone=false;local freddyDone=false;pcall(function() bobDone=string.find(children[5].Text,"1/1")~=nil ;freddyDone=string.find(children[6].Text,"1/1")~=nil ;end);return bobDone and freddyDone ;end if  not hasQuest() then local npc=workspace.Ignore.NPCs.Quest:FindFirstChild("Chill Billy");if npc then warpToNPC(npc);task.wait(0.5);end repeat if  not _G.AutoMakeEmChill then break;end if npc then interactNPC(npc);end local dialogue=gui:FindFirstChild("QuestGui") and gui.QuestGui:FindFirstChild("Dialogue") ;if (dialogue and dialogue.Visible) then pcall(function() local option=dialogue.Options:FindFirstChild("Option");if (option and option.Visible) then getconnect(option);task.wait(0.2);getconnect(option);task.wait(0.2);getconnect(option);end end);task.wait(0.3);pcall(function() local leave=dialogue.Options:FindFirstChild("Leave");if leave then getconnect(leave);end end);end task.wait(0.5);until hasQuest() or  not _G.AutoMakeEmChill  if  not _G.AutoMakeEmChill then break;end end task.wait(1);while _G.AutoMakeEmChill and  not questDone()  do character=player.Character or player.CharacterAdded:Wait() ;HRP=character:FindFirstChild("HumanoidRootPart");Humanoid=character:FindFirstChildOfClass("Humanoid");Backpack=player:FindFirstChild("Backpack");local Tool=character:FindFirstChild("Kogatana") or Backpack:FindFirstChild("Kogatana") ;if (Tool and Tool:IsA("Tool")) then pcall(function() Humanoid:EquipTool(Tool);end);end local aliveFolder=workspace:FindFirstChild("Alive");if aliveFolder then for _,mob in ipairs(aliveFolder:GetChildren()) do if ( not _G.AutoMakeEmChill or questDone()) then break;end if (mob and mob.Parent and (string.find(mob.Name,"Angry Bob") or string.find(mob.Name,"Angry Freddy"))) then local mobHumanoid=mob:FindFirstChildOfClass("Humanoid");local mobHRP=mob:FindFirstChild("HumanoidRootPart");if (mobHumanoid and mobHRP and (mobHumanoid.Health>0)) then while _G.AutoMakeEmChill and  not questDone() and mob.Parent and mobHumanoid and mobHRP and (mobHumanoid.Health>0)  do if  not mobHRP.Parent then break;end if (mobHumanoid.Health<=(mobHumanoid.MaxHealth * 0.7)) then mobHumanoid.Health=0;break;end local behindPos=(mobHRP.Position-(mobHRP.CFrame.LookVector * 1)) + Vector3.new(0,1,0) ;pcall(function() HRP.CFrame=CFrame.new(behindPos) * (mobHRP.CFrame-mobHRP.Position) ;end);local EquippedTool=character:FindFirstChild("Kogatana");if  not EquippedTool then local ToolInBag=Backpack:FindFirstChild("Kogatana");if ToolInBag then pcall(function() Humanoid:EquipTool(ToolInBag);end);end end pcall(function() local CurrentTool=character:FindFirstChild("Kogatana");if CurrentTool then CurrentTool:Activate();end end);task.wait();mobHumanoid=mob:FindFirstChildOfClass("Humanoid");mobHRP=mob:FindFirstChild("HumanoidRootPart");if ( not mobHumanoid or  not mobHRP) then break;end end task.wait();end end end else task.wait(1);end task.wait();end if  not _G.AutoMakeEmChill then break;end local npc=workspace.Ignore.NPCs.Quest:FindFirstChild("Chill Billy");if npc then warpToNPC(npc);task.wait(0.5);interactNPC(npc);task.wait(0.5);clickDialogue();end task.wait(1);task.wait(6);end end);end end);ChillToggle:SetValue(false);local TraitorToggle=Tabs.Quest:AddToggle("AutoTraitorQuest",{Title="Auto No Good Time for Traitors",Default=false});_G.AutoTraitorQuest=false;TraitorToggle:OnChanged(function(state) _G.AutoTraitorQuest=state;if state then task.spawn(function() while _G.AutoTraitorQuest do local player=game:GetService("Players").LocalPlayer;local character=player.Character or player.CharacterAdded:Wait() ;local HRP=character:FindFirstChild("HumanoidRootPart");local Humanoid=character:FindFirstChildOfClass("Humanoid");local Backpack=player:FindFirstChild("Backpack");local gui=player:WaitForChild("PlayerGui");local function hasQuest() local ok,result=pcall(function() return gui.QuestGui.QuestsFrame.QuestName.Text;end);return ok and (result=="No Good Time for Traitors") ;end local function warpToNPC(npc) if (character:FindFirstChild("HumanoidRootPart") and npc:FindFirstChild("HumanoidRootPart")) then character.HumanoidRootPart.CFrame=npc.HumanoidRootPart.CFrame * CFrame.new(0,0,3) ;end end local function interactNPC(npc) for _,v in pairs(npc:GetDescendants()) do if v:IsA("ProximityPrompt") then pcall(function() v.MaxActivationDistance=math.huge;v.HoldDuration=0;v.RequiresLineOfSight=false;fireproximityprompt(v);end);end if v:IsA("ClickDetector") then pcall(function() fireclickdetector(v);end);end end end local function clickDialogue() local dialogue=gui:FindFirstChild("QuestGui") and gui.QuestGui:FindFirstChild("Dialogue") ;if (dialogue and dialogue.Visible) then pcall(function() local option=dialogue.Options:FindFirstChild("Option");if (option and option.Visible) then getconnect(option);task.wait(0.2);getconnect(option);task.wait(0.2);getconnect(option);end end);task.wait(0.3);pcall(function() local leave=dialogue.Options:FindFirstChild("Leave");if leave then getconnect(leave);end end);end end local function questDone() local ok,result=pcall(function() return gui.QuestGui.QuestsFrame.QuestsScroll:GetChildren()[5].Text;end);if  not ok then return false;end return string.find(result,"1/1")~=nil ;end if  not hasQuest() then local npc=workspace.Ignore.NPCs.Quest:FindFirstChild("Bandits Leader");if npc then warpToNPC(npc);task.wait(0.5);end repeat if  not _G.AutoTraitorQuest then break;end if npc then interactNPC(npc);end local dialogue=gui:FindFirstChild("QuestGui") and gui.QuestGui:FindFirstChild("Dialogue") ;if (dialogue and dialogue.Visible) then pcall(function() local option=dialogue.Options:FindFirstChild("Option");if (option and option.Visible) then getconnect(option);task.wait(0.2);getconnect(option);task.wait(0.2);getconnect(option);end end);task.wait(0.3);pcall(function() local leave=dialogue.Options:FindFirstChild("Leave");if leave then getconnect(leave);end end);end task.wait(0.5);until hasQuest() or  not _G.AutoTraitorQuest  if  not _G.AutoTraitorQuest then break;end end task.wait(1);while _G.AutoTraitorQuest and  not questDone()  do character=player.Character or player.CharacterAdded:Wait() ;HRP=character:FindFirstChild("HumanoidRootPart");Humanoid=character:FindFirstChildOfClass("Humanoid");Backpack=player:FindFirstChild("Backpack");local Tool=character:FindFirstChild("Kogatana") or Backpack:FindFirstChild("Kogatana") ;if (Tool and Tool:IsA("Tool")) then pcall(function() Humanoid:EquipTool(Tool);end);end local aliveFolder=workspace:FindFirstChild("Alive");if aliveFolder then for _,mob in ipairs(aliveFolder:GetChildren()) do if ( not _G.AutoTraitorQuest or questDone()) then break;end if (mob and mob.Parent and string.find(mob.Name,"Bandit Traitor")) then local mobHumanoid=mob:FindFirstChildOfClass("Humanoid");local mobHRP=mob:FindFirstChild("HumanoidRootPart");if (mobHumanoid and mobHRP and (mobHumanoid.Health>0)) then while _G.AutoTraitorQuest and  not questDone() and mob.Parent and mobHumanoid and mobHRP and (mobHumanoid.Health>0)  do if  not mobHRP.Parent then break;end if (mobHumanoid.Health<=(mobHumanoid.MaxHealth * 0.7)) then mobHumanoid.Health=0;break;end local behindPos=(mobHRP.Position-(mobHRP.CFrame.LookVector * 1)) + Vector3.new(0,1,0) ;pcall(function() HRP.CFrame=CFrame.new(behindPos) * (mobHRP.CFrame-mobHRP.Position) ;end);local EquippedTool=character:FindFirstChild("Kogatana");if  not EquippedTool then local ToolInBag=Backpack:FindFirstChild("Kogatana");if ToolInBag then pcall(function() Humanoid:EquipTool(ToolInBag);end);end end pcall(function() local CurrentTool=character:FindFirstChild("Kogatana");if CurrentTool then CurrentTool:Activate();end end);task.wait();mobHumanoid=mob:FindFirstChildOfClass("Humanoid");mobHRP=mob:FindFirstChild("HumanoidRootPart");if ( not mobHumanoid or  not mobHRP) then break;end end task.wait();end end end else task.wait(1);end task.wait();end if  not _G.AutoTraitorQuest then break;end local npc=workspace.Ignore.NPCs.Quest:FindFirstChild("Bandits Leader");if npc then warpToNPC(npc);task.wait(0.5);interactNPC(npc);task.wait(0.5);clickDialogue();end task.wait(1);task.wait(6);end end);end end);TraitorToggle:SetValue(false);local GemToggle=Tabs.Quest:AddToggle("AutoGemHunter",{Title="Auto Gem Hunter",Default=false});_G.AutoGemHunter=false;GemToggle:OnChanged(function(state) _G.AutoGemHunter=state;if state then task.spawn(function() while _G.AutoGemHunter do local player=game:GetService("Players").LocalPlayer;local character=player.Character or player.CharacterAdded:Wait() ;local gui=player:WaitForChild("PlayerGui");local function hasQuest() local ok,result=pcall(function() return gui.QuestGui.QuestsFrame.QuestName.Text;end);return ok and (result=="Gem Hunter") ;end local function warpToNPC(npc) if (character:FindFirstChild("HumanoidRootPart") and npc:FindFirstChild("HumanoidRootPart")) then character.HumanoidRootPart.CFrame=npc.HumanoidRootPart.CFrame * CFrame.new(0,0,3) ;end end local function interactNPC(npc) for _,v in pairs(npc:GetDescendants()) do if v:IsA("ProximityPrompt") then pcall(function() v.MaxActivationDistance=math.huge;v.HoldDuration=0;v.RequiresLineOfSight=false;fireproximityprompt(v);end);end if v:IsA("ClickDetector") then pcall(function() fireclickdetector(v);end);end end end local function clickDialogue() local dialogue=gui:FindFirstChild("QuestGui") and gui.QuestGui:FindFirstChild("Dialogue") ;if (dialogue and dialogue.Visible) then pcall(function() local option=dialogue.Options:FindFirstChild("Option");if (option and option.Visible) then getconnect(option);task.wait(0.2);getconnect(option);task.wait(0.2);getconnect(option);end end);task.wait(0.3);pcall(function() local leave=dialogue.Options:FindFirstChild("Leave");if leave then getconnect(leave);end end);end end local function questDone() local ok,result=pcall(function() return gui.QuestGui.QuestsFrame.QuestsScroll:GetChildren()[5].Text;end);if  not ok then return false;end return string.find(result,"1/1")~=nil ;end if  not hasQuest() then local npc=workspace.Ignore.NPCs.DailyQuest:FindFirstChild("Gemologist");if npc then warpToNPC(npc);task.wait(0.5);end repeat if  not _G.AutoGemHunter then break;end if npc then interactNPC(npc);end local dialogue=gui:FindFirstChild("QuestGui") and gui.QuestGui:FindFirstChild("Dialogue") ;if (dialogue and dialogue.Visible) then pcall(function() local option=dialogue.Options:FindFirstChild("Option");if (option and option.Visible) then getconnect(option);task.wait(0.2);getconnect(option);task.wait(0.2);getconnect(option);end end);task.wait(0.3);pcall(function() local leave=dialogue.Options:FindFirstChild("Leave");if leave then getconnect(leave);end end);end task.wait(0.5);until hasQuest() or  not _G.AutoGemHunter  if  not _G.AutoGemHunter then break;end end task.wait(1);while _G.AutoGemHunter and  not questDone()  do character=player.Character or player.CharacterAdded:Wait() ;local HRP=character:FindFirstChild("HumanoidRootPart");local gem=workspace.Chests.Gemologist.TreasureChest:FindFirstChild("Gem");if (gem and HRP) then pcall(function() HRP.CFrame=CFrame.new(gem.Position + Vector3.new(0,3,0) );end);end task.wait(0.2);end if  not _G.AutoGemHunter then break;end local npc=workspace.Ignore.NPCs.DailyQuest:FindFirstChild("Gemologist");if npc then warpToNPC(npc);task.wait(0.5);interactNPC(npc);task.wait(0.5);clickDialogue();end task.wait(1);task.wait(6);end end);end end);GemToggle:SetValue(false);SaveManager:SetLibrary(Fluent);InterfaceManager:SetLibrary(Fluent);SaveManager:IgnoreThemeSettings();SaveManager:SetIgnoreIndexes({});InterfaceManager:SetFolder("FinzerHub");SaveManager:SetFolder("FinzerHub/specific-game");InterfaceManager:BuildInterfaceSection(Tabs.Settings);SaveManager:BuildConfigSection(Tabs.Settings);Window:SelectTab(1);Fluent:Notify({Title="Fluent",Content="The script has been loaded.",Duration=8});SaveManager:LoadAutoloadConfig(); end
